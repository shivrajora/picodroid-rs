# Survey build only (not the root build, which has no gradle.properties).
kotlin.jvm.target.validation.mode=error
# Deterministic out/: every dump run recompiles from scratch.
kotlin.incremental=false
org.gradle.jvmargs=-Xmx1g
kotlin.daemon.jvmargs=-Xmx1g
