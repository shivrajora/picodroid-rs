# Vendored littlefs-rust v0.1.0 (BSD-3-Clause) with one change: the per-open
# file cache is reserved fallibly (src/file.rs), so a full heap is a NoMemory
# error instead of an abort. Wired via [patch.crates-io] in the workspace.
#
# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
rust-version = "1.87"
name = "littlefs-rust"
version = "0.1.0"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Safe Rust API for the LittleFS embedded filesystem"
readme = "README.md"
keywords = [
    "littlefs",
    "filesystem",
    "embedded",
    "no-std",
    "flash",
]
categories = [
    "embedded",
    "filesystem",
    "no-std",
]
license = "BSD-3-Clause"
repository = "https://github.com/light-player/littlefs-rust"

[features]
alloc = ["littlefs-rust-core/alloc"]
default = ["alloc"]
log = ["littlefs-rust-core/log"]
std = ["alloc"]

[lib]
name = "littlefs_rust"
path = "src/lib.rs"

[dependencies.bitflags]
version = "2"

[dependencies.littlefs-rust-core]
version = "0.1"

