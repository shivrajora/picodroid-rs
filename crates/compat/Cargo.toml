[package]
name = "compat"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-only"
description = "Picodroid PAPK ↔ firmware framework-map-version compatibility check. Single source of truth shared by pico-jvm (device) and tools/pdb (host)."
readme = "README.md"

[lib]
path = "src/lib.rs"
