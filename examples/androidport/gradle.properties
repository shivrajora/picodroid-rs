# This app is written entirely against android.* imports. compatAliases makes
# the picodroid SDK available under the android.* namespace at compile time and
# rewrites the resulting android/* bytecode references to the real picodroid/*
# classes at pack time. Without it the app would not compile or load.
picodroid.compatAliases=true
